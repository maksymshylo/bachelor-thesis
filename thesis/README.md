# Bachelor Thesis

This directory contains the LaTeX version of the bachelor thesis. It follows the same structure and Docker build workflow as the master thesis.

From this directory run `bash build.sh`. To use Times New Roman from `.fonts`, run `THESIS_FONT=times bash build_with_bib.sh`.
